server:
  port: 8000
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/anthonyZR11/microservicios-vehicles.git
          search-paths: config-data
  application:
    name: config-server